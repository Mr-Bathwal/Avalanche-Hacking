# Copy to .env before deploying to a live network.

# Deployer private key (NEVER commit the real .env). Testnet key only.
PRIVATE_KEY=your_testnet_private_key

# Avalanche Fuji RPC (public default works without a key).
FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc
